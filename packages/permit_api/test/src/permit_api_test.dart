// ignore_for_file: prefer_const_constructors
import 'package:permit_api/permit_api.dart';
import 'package:test/test.dart';

void main() {
  group('PermitApi', () {
    test('can be instantiated', () {
      expect(PermitApi(), isNotNull);
    });
  });
}
