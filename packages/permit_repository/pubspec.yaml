name: permit_repository
description: A repository that handles permit related requests.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  permit_api:
    path: ../permit_api

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.8
  very_good_analysis: ^6.0.0
