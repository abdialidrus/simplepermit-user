// ignore_for_file: prefer_const_constructors

import 'package:app_themes/app_themes.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AppThemes', () {
    test('can be instantiated', () {
      expect(AppThemes(), isNotNull);
    });
  });
}
